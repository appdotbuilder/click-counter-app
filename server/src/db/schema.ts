// Since the click counter is ephemeral and resets on refresh,
// we don't need any database tables for this application.
// The counter state will be maintained in memory only.

// Export empty tables object to maintain consistency with the project structure
export const tables = {};